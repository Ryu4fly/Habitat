<div class="diary">
  <div class="title">
    <p id="back"><%= link_to "Back", profile_path %></p>
    <h1 id="diary">Diary</h1>
  </div>
  <div class="entries">
    <% if @entries.empty? %>
      <p>No entries logged yet!</p>
    <% else %>
      <% @entries.each do |entry| %>
        <%= link_to entry_path(entry) do %>
          <div class="entry-card">
            <div class="text">
              <% if entry.cig_smoked == 0 %>
                <div class="dot" style="border-color: #7EF49F"></div>
                <p class="date"><%= entry.date %></p>
                <h5 class="craving-title" style="color: #7EF49F;">Craving overcome</h5>
              <% else %>
                <div class="dot" style="border-color: #68E2F6;"></div>
                <p class="date"><%= entry.date %></p>
                <h5 class="smoked-title" style="color: #68E2F6;">Smoked cigarette</h5>
              <% end %>
            </div>
            <div class="arrow">
              <i class="fas fa-chevron-right"></i>
            </div>
          </div>
        <% end %>
      <% end %>
    <% end %>
  </div>
</div>
